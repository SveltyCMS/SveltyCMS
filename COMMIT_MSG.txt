fix: correct user.remote.ts exports and revert wrapHandle optimization

- Fix production build failure: SvelteKit 5 .remote.ts files require all
  exports to be remote functions (query()). Made buildUpdateProfileBody
  and remoteJsonHeaders local functions instead of exported.
  
- Revert wrapHandle micro-optimization: The direct return wrapper (without
  async/await) caused page content not to render in CI E2E tests. The
  microtask boundary from async/await is needed by SvelteKit's sequence
  function to properly chain handlers.
