---
path: "docs/project/roadmap-2026.mdx"
title: "Project Roadmap 2026"
description: "SveltyCMS 2026 roadmap — upcoming milestones and in-progress work. Completed items move to the achievements log."
order: 99
icon: "mdi:map-clock-outline"
author: "SveltyCMS Team"
created: "2026-01-18"
updated: "2026-09-18"
tags:
  - "roadmap"
  - "todo"
  - "planning"
---

# SveltyCMS: 2026 Roadmap

> [!NOTE]
> **EU Compliance**: Based on publicly available documentation as of June 2026. Performance measurements where cited: self-measured via `BENCHMARK_RECORD=1 bun test tests/benchmarks/`. See [Achievements Log](./achievements-2026.mdx) (truth-first) and [Performance Architecture](../reference/database/performance-architecture.mdx).

This document is the **living roadmap** — in-progress and planned work. Completed achievements: [Achievements Log](./achievements-2026.mdx).

> [!TIP]
> **Recently Completed (September 2026)**: Platform hardening (one mutation limiter, branded `systemScope`, Mongo REST point-read, Yjs entry ACL, same-host sockets, client-safe tenant split, session AMR persistence, and adaptive smart rate-limiting) has shipped and is fully logged in [Achievements Log](./achievements-2026.mdx) §3.16.

---

## 📊 CMS Tables & List Platform (Smart Table)

**Shipped foundation** (July 2026): headless controller, schema-aware server filters, QueryBuilder IR, SWR/cache hash, shared UX primitives — plus the July 2026 2FA hardening set (AES-256-GCM TOTP secrets at rest, resumable enrollment, trusted-devices cookie, configurable TOTP window, `afterAuthenticate` plugin hook). Both are documented in the [Achievements Log](./achievements-2026.mdx). Collections stay **server-secure**.

### Next (high leverage — foundations ready)

| Enhancement                      | Status     | Notes                                                                                                       |
| -------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------- |
| **Saved views**                  | 🟢 Shipped | 100% wired across all list surfaces: Entry-list, website-tokens, users, and media                           |
| **Column resize + layout prefs** | 🟡 Partial | Entry-list + tokens `layoutKey`; extend media/users                                                         |
| **Status / domain facets**       | 🟢 Shipped | Collection facets + interactive User Management facet chips (`All`, `Admins`, `Users`, `Active`, `Blocked`) |
| **List metrics**                 | 🟢 Shipped | `list-query-metrics` + SSR badge on collections, users, and tokens                                          |
| **Filter engine reuse**          | 🟢 Shipped | Allowlisted field filtering (`role`, `blocked`, `email`, `username`) on users/tokens                        |

### Planned product UX

- **Bulk field-edit UI**, sticky columns, multi-column sort, advanced filter operators UI
- **Export filtered selection**, keyboard grid nav (ATAG), responsive card mode
- **Team / cloud saved views** (enterprise)

### Performance & observability

- Predictive preload “warm next page” — 🟢 Shipped: sidebar smart-preload of learner-predicted path + dashboard hot-collection chips + heat-aware SWR + Smart Table pagination links with `data-preload="viewport"` (2026-09-02, see [Achievements Log](./achievements-2026.mdx)).
- List p50/p95 from `list-query-metrics` into benchmark trend notes
- Negative cache / empty-page policy documentation

### Explicitly deferred

| Item                         | Why                         |
| ---------------------------- | --------------------------- |
| TanStack as collection core  | SSR + multi-tenant security |
| Mini matrices on Smart Table | Stay purpose-built          |

---

## 🏗️ In Progress

- 🏗️ **AI-Assisted Builder (Phase 0 shipped, Phase 1 in progress)** — `src/services/ai-builder/` gateway skeleton: `gateway.ts` (ordered backend failover via the existing Ollama `aiService` + per-user sliding-window quotas), `prompts.ts` (`<user_data>` passive-reference injection shield), `validator.ts` (Valibot structured-output validation + widget-registry awareness + reserved `db_fieldName` blocking), `diff.ts` (proposal-vs-schema diff), `collection-designer.ts` (design/refine). API namespace `ai-builder` registered in the existing fail-closed dispatcher (`ENDPOINT_PERMISSIONS: system:settings`, admin-gated handler): `POST /api/ai-builder/design-collection` and `refine-collection` live; `approve-collection` returns 501 until Phase 1 (schema write via AST path). See [AI-Assisted Builder Design](../development/ai-builder-design.mdx).

- 🏗️ **Marketplace Phase 2 (in-app catalog)** — **August 2026 progress:** Extensions page **Marketplace** tab + `marketplace-view.svelte` + `GET /api/marketplace` offline-first merge (local listings + `marketplace-client` remote). Dashboard widgets refactored into **marketplace-portable package folders** (`widgets/<folder>/<component>.svelte` + required `widget.json` + `.mdx`) with `installDashboardWidget()` and local catalog listings (type filter incl. `dashboard`, license/price badges). **Licensing aligned with widgets/plugins/site starter**: Free / Freemium (14-day key-less trial) / Paid via `checkExtensionLicense("dashboard", id)` — client-side upgrade prompts **and** server-side `403 LICENSE_REQUIRED` gates on premium dashboard endpoints (see [license-endpoint-inventory](../reference/security/license-endpoint-inventory.mdx)). Still open: one-click install pipeline for remote packages, richer package cards, in-app license UX.
- 🏗️ **Plugin Admin Pages (`page` part)** — plugins contribute full admin pages under `/plugin/<path>` with server `load`, RBAC gating (403), declarative sidebar nav, and the `<AdminZone>` renderer for `adminTool` + `AdminAreaExtension` zones. See [Plugin Architecture](../development/plugins/architecture.mdx).
- 🏗️ **Published DB Adapters**: `sveltycms-sqlite|mongodb|mariadb|postgresql` npm packages — extraction + CI publish pending.
- 🏗️ **Native commerce pipeline** — optional Commerce plugin (disabled by default): tenant-scoped guest cart, merge on login, quotes, inventory, checkout, `/api/commerce/*`, Stripe `grandTotal` intent, `/shop` `/cart` `/checkout` `/account`, transactional mail. Smart Importer Woo/Shopify. Dashboard Orders / Inventory widgets. Dedicated `/api/commerce` rate-limit lane. **Paid add-ons (not core):** Live Shipping (`plugin:shipping-live`, UPS/FedEx/DHL) and TaxJar (`plugin:taxjar`) — table rates stay free. **Open (same list as the overview “Still open” table):** `ConditionPlugin` / AND-OR promotion groups; BOGO / tiered promotions, multi-warehouse, FX resolver; Stripe Billing (Pro endpoint is `501`); `computeTotals` &lt; 5 ms benchmark; HTTP cart→order→refund two-tenant integration; Schema.org Product/Offer/Review on `/shop`. Product map: [E-Commerce Overview](../reference/architecture/ecommerce-overview.mdx).

---

## 🚀 Planned

- **Cross-Source Merge Execution (v3.1 → stable)**
- **Visual Studio Query Builder**
- **GraphQL Supergraph / Federation**
- **Svelty Managed Cloud**, Docker/Terraform enterprise pack
- **AI-Verified Zero-Downtime Deployments**
- **SvelteKit 3.0**, **TypeScript 7** — running `@sveltejs/kit@3.0.0-next.27` + `typescript@6.0.3`; TS7 adoption pending SvelteKit upstream `write_tsconfig` modernisation ([language-tools#3063](https://github.com/sveltejs/language-tools/issues/3063))
- **Rust Tooling Watch (`fuzdev/tsv`)** — Track development of [fuzdev/tsv](https://github.com/fuzdev/tsv) (native Rust Svelte parser + formatter); evaluate for Svelte AST compilation & CI linting once its planned linter ships to complement/benchmark against Oxc (`oxlint` + `oxfmt`).
- **Third-party pentest** — engage a pentest lab before enterprise GA (0 CVEs on record today, self-checked via NVD / GitHub Advisory DB)
- **System-language workstream (P0–P4)** — see [below](#system-language-p0-p4). Not started as a completed pack: EN/DE catalogs exist; admin chrome is only partially keyed.

---

## 🧩 Gap Register & Non-Goals (August 2026)

Honest assessment against reference platforms, based on publicly available documentation as of 2026-08-04. Closure paths link to the roadmap items above; shipped items are tracked in the [Achievements Log](./achievements-2026.mdx).

| Capability                               | Reference platforms (public docs)                                                      | Our current state                                                                                                                                                                               | Gap & closure path                                                                                                                         |
| :--------------------------------------- | :------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------- |
| - **Distributed SSO realm sessions**     | Keycloak documents realm-wide session clustering and cross-application SSO propagation | Single-instance session store (L1/L2 + DB); OIDC login (PKCE S256 + JIT provisioning) and SAML plus all three OIDC logout mechanisms shipped                                                    | No cross-application session propagation yet. Closure: distributed session clustering with the managed-cloud / Docker enterprise pack (P2) |
| **Adaptive / risk-based authentication** | Keycloak documents conditional authentication flows (step-up, risk-based)              | RFC 8176 Session AMR + `mfaRequired` role enforcement + step-up trusted device tokens persisted on all 4 DB adapters; static lockout, TTL/idle controls, step-up re-auth for session management | No automated risk-scoring policy engine. Closure: roadmap candidate; the shipped log-only anomaly detection feeds future alerting          |
| **Module ecosystem depth**               | Drupal's public module directory lists tens of thousands of modules                    | Plugin `page` part, widgets, and dashboard widget packages provide first-class admin extension points                                                                                           | Catalog is young. Closure: marketplace in-app catalog + one-click install (P1); ecosystem launch items on the roadmap                      |
| **Field-level write guards**             | Directus documents per-field read/write policy per role                                | Read-side response filtering shipped (`FIELD_PERMISSIONS`); filter/search FLAC shipped (`canAccessField`); mutation enforcement active via `assertWriteAllowed`                                 | **Shipped**: Full read + write field permission enforcement active across SDK and API pipelines                                            |
| **Passkey UI polish**                    | WebAuthn/passkeys widely deployed in consumer-grade UX                                 | WebAuthn/passkey support exists; UI polish pending                                                                                                                                              | Small, contained; tracked in the security scorecard                                                                                        |
| **Independent verification**             | Enterprise platforms publish third-party pentest / audit reports                       | 0 CVEs on record (NVD / GitHub Advisory DB, self-checked); internal scanners enforced in CI                                                                                                     | No third-party pentest yet. Closure: engage a pentest lab before enterprise GA                                                             |

**Strategic non-goals (explicit):** matching WordPress/PHP's install base or module count is not a design target — the extension surface is deliberately typed and tree-shaken (plugins, widgets, dashboard packages) rather than a generic runtime hook bus. **System i18n:** do not replace Paraglide with i18next (compile-time / tree-shake stays); do not unlock ISO codes in the picker as a “50 language” claim while admin chrome still has hardcoded English.

---

## System language P0–P4 (Sept 2026) {#system-language-p0-p4}

Order is fixed: **coverage before packs, packs before cockpit, cockpit before marketing claims.** Status is the honest snapshot, not the goal.

| Pri    | Goal                        | Why it sits here                                                                                                                                                               | Status (2026-09-08)                                                                                                                                                                                                                                         |
| :----- | :-------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0** | Make EN/DE _true_           | ~21% of admin Svelte files import Paraglide; German still hits English on config/media/dashboard/image-editor. New locales would only translate that fifth.                    | **In Progress.** Catalogs EN/DE key-parity (1158). `scripts/check-hardcoded-copy.ts` implemented and gating CI. Wildcard imports replaced with explicit named imports.                                                                                      |
| **P1** | EU pack + AR                | FR, ES, IT, NL, PL cover the EU agency/enterprise set we position for; AR proves RTL (`getTextDirection` + logical CSS), not theory. Prefer 8 reviewed locales over 60 drafts. | **Open.** `src/messages/` is `en.json` + `de.json` only.                                                                                                                                                                                                    |
| **P2** | Remove the compile cliff    | After Save, the operator must not sit on English copy + RTL with no next step. Dev: HMR after Paraglide compile. Prod: explicit “run `bun run paraglide` / redeploy”.          | **Partial.** Settings toast reports added locales / failed translate. No transparent HMR handshake.                                                                                                                                                         |
| **P3** | Unified translation cockpit | Combine `systemLanguage` (Paraglide chrome) with per-field content progress (`translationProgress`) + AI translate. “Admin: DE · Content DE 100% / FR 60% / AR 0%”.            | 🟢 **Shipped (September 2026).** Dual-lens studio cockpit (`translation-cockpit.svelte`) with live Paraglide chrome switcher, per-language readiness tokens, 1-click batch AI translation via `/api/ai/translate`, and interactive deficiency jump & focus. |
| **P4** | UWG-honest buyer copy       | Buyers ask: can editors work in DE/FR _today_? can we add a language without a vendor release? does compile-time stay fast? Engine names (Paraglide vs i18next) are secondary. | **Partial.** Guide + comparison note machine-draft vs Crowdin review. Three buyer questions not yet the lead.                                                                                                                                               |

**After P0:** one `bun translate` across P1 locales, then [Fink](https://fink.inlang.com/github.com/SveltyCMS/SveltyCMS) lektorat (`src/messages/` PRs). Machine seed, community review. See [System Languages](../guides/configuration/system-languages.mdx).

---

## 🏢 Enterprise Workspaces & Governance

- Whole-team studio workflow, editorial state visibility, translation cockpit
- Adaptive workspaces theme, OAuth 2.1 for MCP, deep JSON path filtering
- AI command bar guardrails, Rust tooling evaluation ([fuzdev/tsv](https://github.com/fuzdev/tsv) formatter/parser vs Oxc), onboarding/deploy buttons

---

## 📣 Future Improvements to Get Noticed

### 🔴 High

1. Marketplace static OG sync on live docroot
2. Docs “Was this page helpful?” widget wiring
3. Framework quickstart tabs / role segmentation
4. SEO comparison blogs (dated, EU-compliant)
5. `FUNDING.yml` / sponsors
6. Ecosystem launch (PH, HN, awesome lists)

### 🟡 Medium

7. Versioned docs
8. Per-page OG images
9. Telemetry SSR of first-paint stats
10. Canonical Discord invite
11. Stripe renewal webhook fix
12. Enterprise governance feature page

### GEO / KPIs

13. Agent-prompt onboarding snippets
14. FAQ JSON-LD
15. Verifiable dated technical claims (ongoing — evaluation docs truth-pass Aug 2026)
16. Analytics / Search Console

---

## Priority for implementers (current)

| Priority | Work                                                                                                                                          |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0**   | AI-Assisted Builder Phase 1 (`approve-collection` schema AST generator) & Smart Table bulk field editing / column resize                      |
| **P1**   | Marketplace remote package one-click install pipeline; published database adapter packages (`sveltycms-sqlite\|mongodb\|mariadb\|postgresql`) |
| **P2**   | Docker/Terraform enterprise cluster pack, managed cloud deployment                                                                            |

## ✅ Shipped (September 2026) — moved to the [Achievements Log](./achievements-2026.mdx)

- **Session AMR / MFA persistence** across all 4 database engines (SQLite, PostgreSQL, MariaDB, MongoDB) with proof-preserving rotation and zero query cost.
- **Adaptive smart rate-limiting & load shedding** — dynamic EWMA system pressure, request velocity damping, and tier/plan quotas under unified `rateLimit()` core.
- **Platform infrastructure** — same-host Unix domain socket auto-detection, client-safe tenant split, and Yjs collaboration route export isolation.
- **Hardware-adaptive profile & 4-DB benchmark matrix** — 200/200 green across all 4 database adapters.

_Last roadmap sync: 2026-09-18_

## ✅ Shipped Milestones

Historical milestones, benchmark proofs, and completed deliverables are permanently maintained in the [Achievements Log](./achievements-2026.mdx).
