---
path: "docs/guides/development/plugin/architecture.mdx"
title: Plugin Architecture
description: Comprehensive guide to the SveltyCMS Enterprise Plugin System — slots, hooks, migrations, and the config-page pattern.
icon: mdi:puzzle-outline
order: 10
author: "SveltyCMS Team"
created: 2026-02-05
updated: "2026-05-31"
tags:
  - plugins
  - architecture
  - guide
  - extensions
---

# Plugin Architecture

SveltyCMS features a robust, enterprise-grade plugin architecture for deep customization without modifying the core. Plugins leverage **Slots** for UI injection, **Lifecycle Hooks** for CRUD interception, **Migrations** for database provisioning, and a **Sandbox** for security isolation.

---

## System Overview

````mermaid
graph TD
    subgraph "Plugin System"
        Registry["Plugin Registry"]
        Plugin["Plugin Definition"]
        Sandbox["Sandbox (5s timeout, 100 queries)"]
    end

    subgraph "Host Application"
        ConfigPage["Config Page (+page.svelte)"]
        SlotComp["Slot.svelte"]
        HookRunner["Hook Runner (CRUD)"]
    end

    subgraph "Data Layer"
        Content["Content System"]
        Migrations["Plugin Migrations"]
        MV["Materialized Views"]
    end

    Plugin -->|registers with| Registry
    Plugin -->|defines| Migrations
    Plugin -->|intercepts| HookRunner
    ConfigPage -->|provides admin UI for| Plugin
    SlotComp -->|renders from| Plugin
    HookRunner -->|reads/writes via| Content
    Sandbox -->|wraps| HookRunner
    Migrations -->|provisions| MV
```text

## System Discovery & Autoloading

SveltyCMS dynamically scans and registers plugins on system boot. There are no static hardcoded imports of plugins in the core codebase.

- **Vite Dev/Build**: Scans all `src/plugins/*/index.ts` paths using Vite's native eager glob tool `import.meta.glob("./*/index.ts", { eager: true })`.
- **Bun/Node runtime (CLI/Vitest)**: Utilizes a resilient directory scanning fallback (`fs.readdirSync`) to import plugins dynamically.

This allows any custom or optional plugin to be added to or deleted from the `src/plugins/` directory without causing compile-time compile/build errors.

---

## Plugin Structure

Every plugin lives in `src/plugins/{plugin-id}/` and follows this structure:

```text
src/plugins/my-plugin/
├── index.ts                  ← Plugin definition + metadata
├── index.server.ts           ← Server-only: hooks, migrations (optional)
├── server/                   ← Server-side services (optional)
│   └── service.ts
├── ui/                       ← Svelte components (optional)
│   └── MyComponent.svelte
└── my-plugin.mdx             ← Plugin documentation
```text

---

## Plugin Interface

```typescript
export interface Plugin {
  /** Required: name, version, icon, category */
  metadata: PluginMetadata;

  /** Optional: public/private settings schema */
  config?: PluginConfig;

  /** Optional: CRUD interception (beforeSave, afterSave, etc.) */
  hooks?: PluginLifecycleHooks;

  /** Optional: server-side entry data enrichment */
  ssrHook?: PluginSSRHook;

  /** Optional: UI columns, actions, edit tabs, sidebar slots */
  ui?: PluginUIContribution;

  /** Optional: database table provisioning */
  migrations?: PluginMigration[];

  /** Optional: restrict plugin to specific collections */
  enabledCollections?: string[];
}
```text

---

## Injection Zones

Plugins inject UI components into predefined zones:

| Zone                 | Location                    | Used By                     |
| -------------------- | --------------------------- | --------------------------- |
| `dashboard`          | Main dashboard widgets      | PageSpeed, Stripe           |
| `sidebar`            | Admin sidebar navigation    | Editable Website            |
| `entry_edit`         | Entry editor tabs/panels    | Any content plugin          |
| `entry_edit_sidebar` | Entry editor sidebar        | SEO plugins                 |
| `entry_edit_header`  | Entry editor header actions | Workflow actions            |
| `config`             | System configuration area   | Stripe, any settings plugin |
| `entry_list_actions` | Entry list action buttons   | PageSpeed refresh           |

---

## Lifecycle Hooks

```typescript
hooks: {
  /** Transform or validate data before it's saved. Must return data. */
  beforeSave: async (context, collection, data) => { return data; },

  /** Trigger side effects after a successful save. */
  afterSave: async (context, collection, result) => {},

  /** Guard or archive before deletion. */
  beforeDelete: async (context, collection, id) => {},

  /** Cleanup related data after deletion. */
  afterDelete: async (context, collection, id) => {},
}
```text

---

## Sandbox Boundaries

All plugin hooks execute within a sandbox (`src/plugins/sandbox.ts`):

| Boundary                  | Limit                                                        | Why                      |
| ------------------------- | ------------------------------------------------------------ | ------------------------ |
| **Collection access**     | Only `plugin_{id}_*` for writes                              | Prevent data corruption  |
| **Protected collections** | `users`, `sessions`, `tokens`, `roles`, `audit_logs` blocked | Never access auth data   |
| **Query count**           | 100 queries per hook                                         | Prevent resource abuse   |
| **Timeout**               | 5 seconds per hook                                           | Prevent hangs            |
| **Error boundary**        | Catches all errors                                           | Plugin crash ≠ CMS crash |

---

## The Config Page + Plugin Pattern

The recommended architecture for admin-facing plugins uses TWO layers:

```text
┌──────────────────────────────────────────┐
│  Config Page (routes/(app)/config/...)    │  ← Admin UI
│  +page.svelte, +page.server.ts            │     CRUD forms, list views
├──────────────────────────────────────────┤
│  Plugin (src/plugins/...)                 │  ← Business logic
│  hooks, migrations, services              │     Auto-behaviors, cache sync
└──────────────────────────────────────────┘
```text

**Example: Redirect Manager**

| Layer             | File                               | Purpose                                     |
| ----------------- | ---------------------------------- | ------------------------------------------- |
| Config Page       | `config/redirects/+page.svelte`    | Manual CRUD for redirect rules              |
| Plugin hooks      | `redirect-manager/index.server.ts` | Auto-redirect on slug change                |
| Plugin migrations | `redirect-manager/index.server.ts` | Creates `redirects` + `redirects_mv` tables |
| Plugin services   | `redirect-manager/index.server.ts` | Edge KV sync, Materialized View sync        |

**The config page calls `LocalCMS` to write data. The plugin's `afterSave` hook fires automatically, handling cache invalidation and sync.** This separation keeps business logic in the plugin while the config page handles UI.

---

## Available Plugins

| Plugin               | Category    | Status | Documentation                                                             |
| -------------------- | ----------- | ------ | ------------------------------------------------------------------------- |
| **PageSpeed**        | Performance | v1.1.0 | [pagespeed.mdx](src/plugins/pagespeed/pagespeed.mdx)                      |
| **Editable Website** | Editing     | Stable | [editable-website.mdx](src/plugins/editable-website/editable-website.mdx) |
| **Redirect Manager** | SEO         | Stable | [redirect-manager.mdx](src/plugins/redirect-manager/redirect-manager.mdx) |
| **Sitemap**          | SEO         | Stable | [sitemap.mdx](src/plugins/sitemap/sitemap.mdx)                            |
| **WebMCP**           | AI          | Stable | [webmcp.mdx](src/plugins/webmcp/webmcp.mdx)                               |
| **Cookie Consent**   | Privacy     | Stable | [cookie-consent.mdx](src/plugins/cookie-consent/cookie-consent.mdx)       |
| **Stripe**           | Payments    | v1.0.0 | [stripe.mdx](src/plugins/stripe/stripe.mdx)                               |

---

## Case Study: Stripe Payments (Implemented)

The Stripe plugin demonstrates a full plugin implementation:

### 1. Configuration

```typescript
config: {
  public: {
    publishableKey: "",   // Client-safe key (pk_...)
    currency: "usd",      // Default currency
  },
  private: {
    secretKey: "",        // Server-only key (sk_...)
    webhookSecret: "",    // Webhook signing secret
  },
}
```text

### 2. Migrations

Creates `plugin_stripe_payments` and `plugin_stripe_customers` collections for transaction records and customer mapping.

### 3. Lifecycle Hooks

```typescript
beforeSave: async (context, _collection, data) => {
  // Verify Stripe payment before allowing content save
  if (data._stripePaymentIntent) {
    const intent = await stripe.paymentIntents.retrieve(data._stripePaymentIntent);
    if (intent.status !== "succeeded") throw new Error("Payment required");
  }
  return data;
};
```text

### 4. UI Components

- `payment-form.svelte` — Stripe Elements card input (PCI-compliant iframe)
- `payment-status.svelte` — Payment status badge in entry lists
- Server endpoints for intent creation and webhook handling

### 5. Optional Dependency

The `stripe` npm package is dynamically imported only when the plugin is enabled — zero overhead when unused.

---

## Best Practices

1. **Lazy Loading**: Use `() => import(...)` for UI components to keep bundle size low.
2. **Type Safety**: Import from `@src/plugins/types` for all interfaces.
3. **Sandbox Compliance**: Stay within 100 queries and 5s timeout per hook.
4. **Collection Prefixing**: Use `plugin_{id}_` for all plugin-owned collections.
5. **Optional Dependencies**: Dynamic import server-side deps so plugins are truly optional.
6. **Documentation**: Every plugin must have an `.mdx` file in its directory.
7. **Config Page Pattern**: Admin-facing plugins should pair with a config page for CRUD.
````

---

## Related

- [Development Guides](../index.mdx)
- [Widget System](../../../architecture/index.mdx)
- [Security Overview](../../../architecture/security/index.mdx)
