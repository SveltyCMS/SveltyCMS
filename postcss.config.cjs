/**
 * @file postcss.config.cjs
 * @description This configuration file sets up PostCSS for the project. It specifies the
 * plugins to be used during the CSS processing pipeline.
 *
 * Note: TailwindCSS v4 is now integrated via Vite plugin, not PostCSS.
 * Autoprefixer is built into TailwindCSS v4.
 *
 * @exports
 * - plugins: An object defining the PostCSS plugins to be used.
 */

module.exports = {
	plugins: {}
};
