{
  "ignorePatterns": ["src/types/**"],
  "rules": {
    "eslint/no-restricted-imports": [
      "warn",
      {
        "paths": [
          {
            "name": "$app/forms",
            "message": "Use SvelteKit 5 Server Functions instead of $app/forms"
          },
          {
            "name": "bun:test",
            "message": "Vitest is canonical (AGENTS §3.5) — import from \"vitest\". `bun test` is not part of any project script."
          }
        ]
      }
    ],
    "unicorn/explicit-timer-delay": "error",
    "eslint/no-unreachable-loop": "error",
    "unicorn/no-confusing-array-with": "error"
  }
}
