---
path: 'apps/docs/contributing/contributing-docs.mdx'
title: 'Contributing to the Documentation'
description: 'A comprehensive guide on how to contribute to the SveltyCMS documentation in the NX monorepo structure.'
order: 2
icon: 'mdi:file-document-plus-outline'
author: 'admin'
created: '2025-09-28'
updated: '2025-10-21'
tags:
  - 'documentation'
  - 'contributing'
  - 'mdx'
  - 'guide'
  - 'nx-monorepo'
---

# Documentation Contribution Guide

High-quality documentation is the backbone of SveltyCMS. This guide provides a clear, actionable framework for all contributors—both human and AI—to create consistent, high-quality content in our **NX monorepo structure**.

---

## NX Monorepo Structure

SveltyCMS uses an **NX monorepo** with multiple applications. Documentation is organized within the dedicated **docs app**:

```
SveltyCMS/                           # Workspace root
├── apps/
│   ├── cms/                         # CMS application
│   ├── setup/                       # Setup wizard (standalone)
│   ├── docs/                        # 📚 Documentation site (YOU ARE HERE)
│   │   ├── architecture/            # Architecture documentation
│   │   ├── api/                     # API reference docs
│   │   ├── contributing/            # Contributor guides
│   │   ├── database/                # Database documentation
│   │   ├── guides/                  # How-to guides
│   │   ├── testing/                 # Testing documentation
│   │   └── widgets/                 # Widget system docs
│   ├── shared-theme/                # Shared UI theme
│   ├── tests/                       # Test suites
│   └── scripts/                     # Build scripts
└── docs/                            # ⚠️ DEPRECATED - DO NOT USE
```

**Important:** The root `docs/` folder is **deprecated**. All documentation must be in `apps/docs/`.

---

## The Golden Rules: Core Principles

All contributions **must** adhere to these foundational rules.

> 1.  **MDX Only & Correct Locations**: All documentation files must be `.mdx` files and placed only in `apps/docs/` subdirectories.
> 2.  **Centralized API Docs**: All API documentation **must** be located in `apps/docs/api/` to provide a single, unified reference.
> 3.  **Current Functionality Only**: Document **only** what exists in the current codebase. Do not add documentation for planned features or unmerged changes.
> 4.  **Assets in `apps/docs/static/docs`**: All non-MDX assets (images, diagrams) **must** be placed in `apps/docs/static/docs/` and referenced via an absolute path.
> 5.  **NX Workspace Aware**: Use correct relative paths when referencing code from other apps (e.g., `../../cms/src/...`).

---

## Canonical Documentation Locations

To eliminate ambiguity, all documentation must be placed in one of the following locations within `apps/docs/`. No other locations are permitted.

### 1. API Documentation → `apps/docs/api/`

- **What:** The complete reference for all REST API endpoints.
- **Where:** `apps/docs/api/`
- **Why:** Centralizes the entire API surface in one place for a holistic, easy-to-navigate user experience.

### 2. Architecture Documentation → `apps/docs/architecture/`

- **What:** System architecture, design decisions, data flows, initialization workflows.
- **Where:** `apps/docs/architecture/`
- **Why:** Provides deep technical understanding of how SveltyCMS works internally.
- **Examples:**
  - `setup-wizard-architecture.mdx`
  - `initialization-workflow.mdx`
  - `collection-store-dataflow.mdx`

### 3. Widget Documentation → `apps/docs/widgets/`

- **What:** Documentation for the widget system and individual widgets.
- **Where:** `apps/docs/widgets/`
- **Why:** Centralized widget documentation separate from widget implementation code.
- **Note:** Widget docs are in docs app, not co-located with CMS widget code.

### 4. Database Documentation → `apps/docs/database/`

- **What:** Database adapters, schemas, migrations, query methods.
- **Where:** `apps/docs/database/`
- **Why:** Provides comprehensive database layer documentation.

### 5. Testing Documentation → `apps/docs/testing/`

- **What:** Testing strategies, test writing guides, CI/CD documentation.
- **Where:** `apps/docs/testing/`
- **Why:** Helps contributors understand testing approach.

### 6. Guides → `apps/docs/guides/`

- **What:** How-to guides, tutorials, step-by-step instructions.
- **Where:** `apps/docs/guides/`
- **Why:** Practical, task-oriented documentation.

### 7. Contributing Guides → `apps/docs/contributing/`

- **What:** Contributor guidelines, code of conduct, PR guidelines (like this document).
- **Where:** `apps/docs/contributing/`
- **Why:** Onboarding and guiding contributors.

---

## Instructions for Documentation Creation

To maintain consistency and quality, all contributors should adhere to the following workflow and rules.

### Guiding Principles

- **Act as an Expert:** Write as an expert technical writer for the SveltyCMS project.
- **Goal:** Produce clear, accurate, and perfectly formatted documentation that is easy for all users to understand.
- **Voice:** Professional, direct, and concise. Use American English.
- **NX Aware:** Understand the monorepo structure and reference apps correctly.

### Authoring Workflow

1.  **Analyze Context:** Before writing, thoroughly analyze the request, existing code, and related documentation to understand the goal.
2.  **Choose Correct App:** Ensure you're working in `apps/docs/`, not root `docs/`.
3.  **Adhere to Rules:** Follow all rules in this guide, especially the **Golden Rules** and the **Frontmatter Guide**.
4.  **Write Content:** Create the MDX content, focusing on clarity, accuracy, and practical examples.
5.  **Validate Frontmatter:** Ensure the YAML frontmatter is complete, correct, and matches the file's location and content.
6.  **Verify Paths:** Double-check that all asset and internal links use correct paths relative to `apps/docs/`.

### Core Authoring Rules

- **ALWAYS** place documentation in `apps/docs/` subdirectories (never in root `docs/`).
- **ALWAYS** ensure the `path` field in the frontmatter starts with `apps/docs/`.
- **NEVER** create `.md` files. Only create `.mdx` files.
- **NEVER** place images or other assets anywhere except `apps/docs/static/docs/`.
- **NEVER** use root `docs/` folder (it's deprecated).

---

## Documentation Structure Example

This example shows the correct placement for different types of documentation in the NX monorepo.

```
apps/docs/                            # Documentation application
├── architecture/                     # Architecture docs
│   ├── setup-wizard-architecture.mdx # Setup wizard design
│   ├── initialization-workflow.mdx   # System initialization
│   ├── collection-store-dataflow.mdx # Collection data flow
│   ├── cache-system.mdx              # Caching strategy
│   └── database-methods.mdx          # Database layer
│
├── api/                              # API reference (ONLY here)
│   ├── index.mdx                     # API overview
│   ├── Authentication_2FA_API.mdx    # 2FA endpoints
│   ├── Collection_API.mdx            # Collection CRUD
│   ├── User_Token_Management_API.mdx # User/token management
│   ├── Widget_API.mdx                # Widget endpoints
│   ├── Settings_API.mdx              # Settings endpoints
│   ├── Media_API.mdx                 # Media endpoints
│   └── ...                           # Other API docs
│
├── contributing/                     # Contributor docs
│   ├── contributing-docs.mdx         # This file
│   ├── code-of-conduct.mdx           # Code of conduct
│   └── pull-requests.mdx             # PR guidelines
│
├── database/                         # Database documentation
│   ├── Core_Infrastructure.mdx       # Database adapters
│   ├── MongoDB_Methods.mdx           # MongoDB specifics
│   └── ...                           # Other DB docs
│
├── guides/                           # How-to guides
│   ├── installation.mdx              # Installation guide
│   ├── getting-started.mdx           # Getting started
│   └── ...                           # Other guides
│
├── testing/                          # Testing docs
│   ├── testing.mdx                   # Testing overview
│   ├── TESTING_GUIDE.mdx             # Test writing guide
│   └── ...                           # Other test docs
│
├── widgets/                          # Widget documentation
│   ├── index.mdx                     # Widget system overview
│   ├── Creating_Custom_Widgets.mdx   # Widget creation guide
│   └── ...                           # Individual widget docs
│
└── static/docs/                      # Static assets
    ├── architecture/                 # Architecture diagrams
    ├── api/                          # API screenshots
    └── ...                           # Other assets
```

---

## Frontmatter Guide

Every MDX file must begin with a YAML frontmatter block enclosed by `---` lines. This metadata is critical for navigation, search, and display.

### Required Fields

| Field       | Required | Purpose                                                                                        | Example Value                                        |
| ----------- | -------- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| path        | Yes      | Full path from workspace root. Used for routing and identification.                            | `"apps/docs/api/Authentication_2FA_API.mdx"`         |
| title       | Yes      | Main heading (H1). Shown in navigation, browser tab, and search results.                       | `"Two-Factor Authentication API"`                    |
| description | Yes      | One-sentence summary for SEO and previews.                                                     | `"Complete 2FA API endpoint reference."`             |
| order       | Yes      | Position in sidebar navigation relative to siblings. Lower numbers appear first.               | `2`                                                  |
| icon        | Yes      | Material Design Icon with `mdi:` prefix.                                                       | `"mdi:shield-lock"`                                  |
| author      | Yes      | GitHub username of original author.                                                            | `"admin"`                                            |
| created     | Yes      | Creation date in YYYY-MM-DD format.                                                            | `"2025-10-21"`                                       |
| updated     | Yes      | Last significant update date. Update with every meaningful change.                             | `"2025-10-21"`                                       |
| tags        | Yes      | Array of relevant keywords for search and filtering. Use lowercase, kebab-case for multi-word. | `["api", "authentication", "2fa", "totp", "nx-app"]` |

**Note:** The `path` field must always start with `apps/docs/` to reflect the NX monorepo structure.

---

## Managing Assets (Images, Diagrams, Videos)

All non-MDX files must be placed in the `apps/docs/static/docs/` directory to keep documentation folders clean.

### Rules

- **Location**: `apps/docs/static/docs/`
- **Structure**: Mirror the `apps/docs/` directory structure
- **Example**: Image for `apps/docs/api/Authentication_2FA_API.mdx` goes in `apps/docs/static/docs/api/`

### Referencing Assets

Always use absolute paths from the docs app root:

```markdown
![2FA Setup Flow](/docs/api/2fa-setup-flow.png)
```

### Asset Guidelines

- **Formats**: PNG for screenshots, SVG for diagrams/icons
- **Optimization**: Keep files under 500KB
- **Naming**: Use descriptive kebab-case names (e.g., `api-authentication-flow.svg`)
- **Alt Text**: Always provide meaningful alt text for accessibility

---

## Content Best Practices

### Writing Style

- **Clarity**: Use active voice, short sentences, plain language
- **Structure**: Start with intro, use H2/H3 headings, end with summary or next steps
- **Examples**: Provide practical code snippets and real-world examples
- **Consistency**: Follow existing doc style. Use American English.
- **Accessibility**: Add alt text, ensure good contrast, make content scannable

### MDX-Specific Guidance

- **Basic Markdown**: Use standard syntax (bold, italics, lists, links, code blocks with language tags)
- **Code Blocks**: Always specify language for syntax highlighting:

````markdown
```typescript
// TypeScript code here
```
````

- **JSX Components**: Use sparingly, only for interactive elements
- **Escaping**: Use backticks or HTML entities (`&lt;`, `&gt;`) for literal angle brackets

---

---

## Review and Update Process

### Pull Request Workflow

1. **Create Branch**: Make documentation changes in a feature branch
2. **Run Linter**: Execute `bun apps/docs/lint-docs.ts` and fix any errors
3. **Submit PR**: Create pull request with descriptive title
4. **Review**: Reviewers check for accuracy, clarity, and adherence to guidelines
5. **Update `updated` field**: Change the date in frontmatter
6. **Merge**: Once approved, changes are merged

### Updating Existing Docs

- Always update the `updated` field in frontmatter
- Keep changes focused on one topic per PR
- Add a commit message explaining what changed

### Versioning

- Documentation is versioned with the code
- Major changes may need release notes
- Breaking API changes must be documented immediately

---

## Checklist for New Documentation

Before submitting a PR:

- [ ] **Correct Location**: The file is in `apps/docs/` subdirectories (not root `docs/`).
- [ ] Frontmatter is complete with all required fields.
- [ ] `path` field starts with `apps/docs/` and matches actual file location.
- [ ] Content is clear, accurate, and follows best practices.
- [ ] Code examples are tested and working.
- [ ] Assets are in `apps/docs/static/docs/` with correct structure.
- [ ] File is `.mdx` format (not `.md`).
- [ ] Linter passes: `bun apps/docs/lint-docs.ts`.
- [ ] `updated` field reflects current date.
- [ ] Tags are relevant and lowercase/kebab-case.
- [ ] Links to other docs use correct paths.
- [ ] Tested rendering in the docs app (if possible).
- [ ] No references to deprecated root `docs/` folder.

---

## Tools and Validation

### Frontmatter Linter

To ensure all documentation is valid, run the linter before submitting a pull request. This tool checks files in `apps/docs/`.

```bash
# From workspace root
bun apps/docs/lint-docs.ts

# Or from docs app
cd apps/docs && bun lint-docs.ts
```

This tool validates all `.mdx` files and reports errors. A passing linter is a requirement for merging.

The linter will:

- ✅ Validate all `.mdx` files in `apps/docs/` directory
- 🔍 Detect `.md` files that need conversion to `.mdx`
- ❌ Report frontmatter errors with helpful messages
- 📊 Display summary with validation results
- ⚠️ Warn about files in deprecated root `docs/` folder

**Always run the linter before submitting a PR.**

---

## NX Monorepo Specific Guidelines

### Referencing Code from Other Apps

When documenting code from other apps in the monorepo, use relative paths:

```markdown
The CMS setup check is in `../../cms/src/utils/setupCheck.ts`.
```

### Cross-App Documentation

- **Setup Wizard**: Document in `apps/docs/architecture/setup-wizard-architecture.mdx`
- **CMS Features**: Document in `apps/docs/` appropriate subdirectory
- **Shared Theme**: Document in `apps/shared-theme/README.md` (not in docs app)
- **Tests**: Document in `apps/docs/testing/`

### Running the Docs App

```bash
# From workspace root
cd apps/docs
bun run dev

# Or using NX
nx dev docs
```

The docs app runs on port 5174 by default.

---

## Migration from Root `docs/` to `apps/docs/`

If you find files in the deprecated root `docs/` folder:

1. **Move the file**: `mv docs/path/file.mdx apps/docs/path/file.mdx`
2. **Update frontmatter `path`**: Change from `docs/...` to `apps/docs/...`
3. **Update internal links**: Search for references and update paths
4. **Test**: Run linter and verify docs app renders correctly
5. **Submit PR**: Include migration in commit message

**Do not create new files in root `docs/`!**

---

## Questions and Support

- **Documentation Issues**: Open an issue on GitHub
- **Unclear Guidelines**: Ask in GitHub Discussions
- **Contribution Help**: Tag maintainers in your PR

---

## Related Documents

- [Code of Conduct](./code-of-conduct.mdx)
- [Pull Request Guidelines](./pull-requests.mdx)
- [API Documentation](../api/index.mdx)
- [Widget System](../widgets/index.mdx)
